#include <bits/stdc++.h>
using namespace std;

bool poss(int n,int k){

    
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        int n,k;
        cin>>n>>k;
        int ans = 0;
        for(int i=1;i<=n;i++){
            if(poss)ans++;
        }
        cout<<ans<<endl;
    }
    return 0;
}
